# === Banco (MySQL/Railway) ===

DATABASE_URL = "mysql://root:niGuGZWucRjqJgUgJJczFUwUNenJGdTr@shortline.proxy.rlwy.net:58424/railway?sslaccept=strict&connection_limit=5"
DIRECT_URL   = "mysql://root:niGuGZWucRjqJgUgJJczFUwUNenJGdTr@shortline.proxy.rlwy.net:58424/railway?sslaccept=strict&connection_limit=5"


# === Network (Railway injeta PORT; deixe assim ou remova) ===
HOST=0.0.0.0
PORT=3000

# === Ambiente/Log ===
NODE_ENV=production
LOG_LEVEL=info
SWAGGER_ENABLED=false

# === CORS ===
# troque pelos domínios reais (NADA de localhost em prod)
CORS_ORIGIN=https://SEU-WEB.DOMAIN,https://SEU-ADMIN.DOMAIN

# === Admin UI (para o QR abrir /checkin?id=...) ===
ADMIN_APP_BASE_URL=https://SEU-ADMIN.DOMAIN

# === Auth (unifique os segredos; remova duplicatas) ===
JWT_SECRET=coloque-uma-chave-bem-grande-e-aleatoria
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=outra-chave-bem-grande-e-aleatoria
JWT_REFRESH_EXPIRES_IN=7d

# === E-mail (se for usar ticket por e-mail) ===
MAIL_FROM=reservas@mane.com.vc
MAIL_FROM_NAME=João do Mané Mercado
# SENDGRID_API_KEY=se-for-usar
# MAIL_CONSULT_BASE=https://SEU-WEB.DOMAIN
# MAIL_LOGO_URL=https://.../logo.png
# ou MAIL_LOGO_BASE64=data:image/png;base64,...

# === Seed inicial (opcional no 1º deploy) ===
ADMIN_NAME=Admin Mané
ADMIN_EMAIL=admin@mane.com.vc
ADMIN_PASSWORD=troque-esta-senha
ADMIN_ROLE=ADMIN

# === QR ===
QR_TTL_HOURS=24

# === Unidades (opcional) ===
# UNITS_JSON=[{"name":"Mané Centro","slug":"mane-centro"},{"name":"Mané Asa Sul","slug":"mane-asa-sul"}]
